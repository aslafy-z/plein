{
  "name": "plein",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/aslafy-z/plein.git"
  },
  "scripts": {
    "dev": "vite",
    "prebuild": "npm run messages",
    "build": "tsc -b && vite build",
    "preview": "npm run build && wrangler dev",
    "messages": "paraglide-js compile --project ./project.inlang --outdir ./src/paraglide --strategy custom-appSettings preferredLanguage baseLocale --emit-ts-declarations",
    "pretypecheck": "npm run messages",
    "typecheck": "tsc -b --noEmit",
    "pretest": "npm run messages",
    "test": "vitest run",
    "pree2e": "npm run messages",
    "e2e": "playwright test",
    "verify:live": "node scripts/live-providers.mjs",
    "build:brands": "node scripts/build-brand-index.mjs",
    "build:icons": "node scripts/build-brand-icons.mjs",
    "build:screenshots": "node scripts/build-screenshots.mjs",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@types/leaflet": "^1.9.21",
    "leaflet": "^1.9.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.47.0",
    "@inlang/paraglide-js": "^2.22.0",
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.1.1",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^6.0.3",
    "esbuild": "^0.28.1",
    "sharp": "^0.35.2",
    "typescript": "^5.6.3",
    "vite": "^8.1.4",
    "vitest": "^4.1.10",
    "wrangler": "^4.114.0"
  },
  "allowScripts": {
    "esbuild@0.28.1": true
  }
}
